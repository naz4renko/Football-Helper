<UserControl x:Class="FootballHelper.WPF.View.ClubShortView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FootballHelper.WPF.View"
             mc:Ignorable="d"
            d:DesignHeight="540" d:DesignWidth="1000">

    <StackPanel VerticalAlignment="Center" Orientation="Horizontal">
        <TextBlock Text="{Binding ClubInfo}"
                   FontFamily="Arial"
                   FontSize="20"
                   FontWeight="Bold"
                   Foreground="Black"/>

        <!--<TextBlock Text="{Binding ClubPoint}"
                   FontFamily="Arial"
                   FontSize="14"
                   FontWeight="Bold"
                   HorizontalAlignment="Center"
                   Grid.Column="1"                   
                   Foreground="Black"/>-->
    </StackPanel>
</UserControl>
